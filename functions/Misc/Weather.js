// Coded by Rhys Douglas. Last updated 25/03/19
module.exports = {
	// Currently broken. API call does not work properly - will investigate other options if neccessary
	getWeather : function(agent) {
		//var weatherinCardiff = "api.openweathermap.org/data/2.5/forecast?id=Cardiff&APPID=7db497620866a7880fa17096101620be"
		//agent.add("In Cardiff it currently is set to be : " + weatherinCardiff.main.temp + "degrees, with " + weatherinCardiff.weather[0].description);
		return;
	}
}